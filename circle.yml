# Language    
  java:
    version: openjdk7
    
    environment:
    foo: bar
    baz: 123

#language: java
#jdk:
 # - oraclejdk8
  #- oraclejdk7

#script:
#  - mvn clean verify
#branches:
 # only:
  #  - develop

#after_success:
 # - bash <(curl -s https://codecov.io/bash)
test:
override:
- tox:
Removing that fixed the issues
test:
override:
- tox
