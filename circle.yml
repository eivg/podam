# Language    
#  java:
 #   version: openjdk7
    
language: java
jdk:
- oraclejdk8
- oraclejdk7

environment:
    foo: bar
    baz: 123
    
script:
  - mvn clean verify
branches:
 only:
  - develop

 after_success:
 - bash <(curl -s https://codecov.io/bash)
